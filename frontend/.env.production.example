# Vercel frontend — public variables only.
# Do not place JWT, database, S3, or Cohere secrets in Vercel.

# Optional: show "Use Nivara sample data" on workstream Information tabs (defaults to on in development)
# NEXT_PUBLIC_ENABLE_NIVARA_SAMPLE_DATA=true

NEXT_PUBLIC_API_BASE_URL=https://<api>.up.railway.app/api/v1
